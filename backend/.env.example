# copie este arquivo para .env e preencha com seus valores reais
# o .env nunca deve ser commitado, está no .gitignore

# chave da API OpenWeatherMap (cadastre em openweathermap.org/api)
OPENWEATHER_API_KEY=

# conexão com o PostgreSQL local
DATABASE_URL=postgresql://usuario:senha@localhost:5432/skytime
